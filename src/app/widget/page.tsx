import type { Metadata } from "next";
import Link from "next/link";
import { myFont } from "@/utils/font";
import Builder from "@/app/widget/Builder";

// Виджет чтений для приходского сайта.
//
// ЗАЧЕМ ЭТО НАМ. Приходских сайтов сотни, и почти на каждом «расписание» — это
// картинка, набранная руками раз в месяц. Виджет даёт им живой день даром, а
// нам — то, без чего не собрать ни агрегатора служб, ни приходской сети:
// знакомство. Подпись со ссылкой в рамке — вся плата, и она названа прямо.
//
// РАМКА, А НЕ СКРИПТ. Скрипт пришлось бы пускать в чужую страницу: свои стили,
// свои запросы, свой ключ, своя порция публичного API — и всякая правка у нас
// могла бы сломать чужой сайт. Рамка рисуется у нас и ничего чужого не трогает.

export const metadata: Metadata = {
    title: "Виджет чтений для сайта прихода — Уставные чтения",
    description:
        "Чтения дня, память и день церковного календаря — рамкой на вашем сайте. "
        + "Одна строка кода, без ключей и без регистрации.",
    openGraph: {
        title: "Виджет чтений для сайта прихода",
        description: "Чтения дня рамкой на вашем сайте: одна строка кода, без ключей.",
        url: "//www.typikon.su/widget/",
    },
};

const Widget = () => (
    <div className={`${myFont.variable} pt-2 flex flex-col gap-5`}>
        <div>
            <h1 className="font-bold font-serif">Виджет чтений для сайта прихода</h1>
            <p className="font-serif text-slate-800 mt-2 max-w-2xl">
                Чтения дня, память и место в церковном календаре — рамкой на вашей странице.
                Обновляется само, каждый день. Ключа не нужно, регистрации тоже: вставьте строку
                кода и забудьте.
            </p>
            <p className="font-serif text-slate-600 text-sm mt-2 max-w-2xl">
                {/* Условие названо прямо и вверху: узнать о нём из чужого кода
                    после установки — худший способ. */}
                Плата одна — подпись со ссылкой на «Уставные чтения» внизу рамки. Убирать её
                нельзя; всё остальное настраивается.
            </p>
        </div>

        <Builder />

        <section className="max-w-2xl">
            <h2 className="font-serif font-bold text-sm">Если хочется рисовать самим</h2>
            <p className="font-serif text-sm text-slate-600 mt-1">
                Те же данные отдаёт публичное API — чтения дня, памяти, зачала и разрешение их в
                шести изданиях Библии:{" "}
                <code className="text-xs">GET /api/v2/calendar/today</code>. Ключ для него не
                обязателен, но с ключом порция больше; условия и описание —{" "}
                <Link href="/api" className="text-red-900 hover:underline">на странице API</Link>.
                Корпус под лицензией CC BY 4.0, и ссылка на источник — то же самое условие, что и
                у рамки.
            </p>
        </section>

        <section className="max-w-2xl">
            <h2 className="font-serif font-bold text-sm">Чего пока нет</h2>
            <p className="font-serif text-sm text-slate-600 mt-1">
                Рамки с расписанием своего храма. Само расписание уже ведётся: найдите храм в{" "}
                <Link href="/temples" className="text-red-900 hover:underline">каталоге</Link>{" "}
                и попросите права — «вести расписание этого храма». Виджет к нему будет
                следующим: он и задуман как продолжение этого.
            </p>
        </section>
    </div>
);

export default Widget;
