import type { Metadata } from "next";
import Link from "next/link";
import { myFont, csFontVariables } from "@/utils/font";
import Form from "@/app/razbor/Form";
import { SITE_URL } from "@/utils/site";

// Разбор набранного текста — для того, кто набирает, а не для того, кто читает.
//
// Приходский наборщик перепечатывает последование из книги, и у него нет
// ничего: ни сверки с собранием, ни способа узнать, что строка набрана с
// опечаткой. Собрание всё это имеет — 226 838 зачинов, словарь ударений,
// разметку колен — и до сих пор держало при себе.

export const metadata: Metadata = {
    title: "Разбор набранного текста — Уставные чтения",
    description:
        "Вставьте набранное последование: покажем, что это за строки, где они в собрании "
        + "и что с ними не так — чужая буква в слове, отсутствие ударений и разметки колен.",
    openGraph: {
        title: "Разбор набранного текста",
        description: "Что это за строки, где они в собрании и что с ними не так.",
        url: `${SITE_URL}/razbor/`,
    },
};

const Razbor = () => (
    <div className={`${myFont.variable} ${csFontVariables} pt-2 flex flex-col gap-5`}>
        <div>
            <h1 className="font-bold font-serif">Разбор набранного текста</h1>
            <p className="font-serif text-slate-800 mt-2 max-w-2xl">
                Вставьте набранное — последование, лист, страницу книги. По каждой строке скажем,
                что это за песнопение и где оно в собрании, а заодно покажем то, чего глазом не
                видно: латинскую букву внутри кириллического слова, отсутствие ударений, нехватку
                разметки колен, повторы.
            </p>
            {/* Оговорка вверху: инструмент говорит «нашлось» и «не нашлось», и
                второе легко принять за приговор набранному. */}
            <p className="font-serif text-slate-600 text-sm mt-2 max-w-2xl">
                <strong>«Не нашлось» — не значит «неверно».</strong> Сличаем точным совпадением
                первых шести слов: строка могла быть набрана с опечаткой, взята из книги, которой
                у нас нет, или переведена иначе. Похожее мы искать не станем — «это, наверное,
                она» хуже честного «не нашлось».
            </p>
        </div>

        <Form />

        <section className="max-w-2xl">
            <h2 className="font-serif font-bold text-sm">Что рядом</h2>
            <p className="font-serif text-sm text-slate-600 mt-1">
                Расставить ударения в тексте — <Link href="/accents" className="text-red-900 hover:underline">
                    раздел «Ударения»</Link>: там спорные места не проставляются молча, а
                показываются списком с частотами, и выбор остаётся за вами. Найти строку по
                первым словам, не разбирая целого текста, — <Link href="/incipits" className="text-red-900 hover:underline">
                    указатель зачинов</Link>.
            </p>
        </section>

        <section className="max-w-2xl">
            <h2 className="font-serif font-bold text-sm">Чего пока нет</h2>
            <p className="font-serif text-sm text-slate-600 mt-1">
                Поиска цитат Писания в присланном тексте. Слой цитат у собрания есть — 531 263
                размеченные ссылки, — но считает его сличитель n-грамм в соседнем проекте, а
                вебу такого указателя не выложено. Обещать это здесь значило бы обещать то, чего
                мы пока не умеем.
            </p>
        </section>
    </div>
);

export default Razbor;
