import Editor from "@/app/calculator/Editor";

const ReadingCalculator = () => {
    return (
        <div className="pt-2">
            <Editor />
        </div>
    );
};

export default ReadingCalculator;
