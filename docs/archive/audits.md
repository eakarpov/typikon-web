# Аудиты кода

Архив сделанного: разделы прежнего `ROADMAP.md` в том виде, в каком они стояли на 2026-09-23. Открытое из них перенесено в [ROADMAP.md](../../ROADMAP.md); здесь текст не правится, кроме относительных ссылок.

## Аудит от 2026-08-13

### Исправлено
- Обход авторизации в admin API (~28 эндпоинтов: nobles, days, months, weeks, books, texts, signs, places) —
  `checkRightsBack()` не прерывал выполнение обработчика при отказе в доступе. Теперь возвращает `boolean`,
  все вызовы обёрнуты в `if (!(await checkRightsBack(req, res))) return;`.
- SQL-инъекция в поиске по SQLite (родословная: nobles/states/families/nationalities/rules, публичный v1 API
  и admin) — строка поиска подставлялась в `LIKE '%${search}%'` напрямую. Заменено на параметризованные запросы.
- Файлы Android-приложения (`public/app/*.apk`) выведены из git — раздували `.git` до 477 МБ с каждым релизом.
  Теперь заливаются на сервер напрямую через nginx (`location /app/` → `/var/www/typikon-app-releases/`),
  см. `release-app.sh`.

### Требует внимания (не исправлено)
- ~~Утечка id_token в логи и два параллельных механизма авторизации~~ — оба пункта держались на
  мёртвом next-auth и сняты одним коммитом 2026-08-24 («Close the housekeeping backlog»):
  `src/app/api/auth/route.ts` удалён, `next-auth` вынут из зависимостей. Логин идёт через Google
  Identity Services → `POST /api/login`, авторизация одна — свой JWT+cookie (`src/lib/authorize/*`).
- ~~Устаревшие зависимости~~ — сверено 2026-09-03: `mongodb` 6.21, `typescript` 5.9.
- ~~Нет тестов и CI~~ — есть с 2026-08-24: 45 файлов тестов (`npm test`) и
  `.github/workflows/ci.yml` на push и pull request. `next build` в CI сознательно не гоняется —
  сборке нужен доступ к Mongo.
- `.git` уже весит 477 МБ из-за истории apk-коммитов — сама история не сожмётся сама по себе;
  переписывание истории (`git filter-repo`/BFG) — отдельная, более рискованная операция, делать только
  по явному запросу и с предупреждением всех, кто клонирует репозиторий.
- **Разделение абзацев в `texts.content` непоследовательно** (обнаружено 2026-08-19, при работе над
  "сообщить об ошибке"): часть импортированных текстов хранит границу абзаца как `"\n \n"` (перевод
  строки, пробел, перевод строки), а не `"\n\n"`. И веб (`ReadingContent.tsx`, `data-paragraph-index`),
  и мобильное приложение (`text_page.dart`, `content.split("\n\n")`) делят по буквальному `"\n\n"` —
  для таких текстов весь текст (тысячи символов) становится одним "абзацем" вместо нескольких. На
  корректность самого функционала (выбор слова/подсветка, статьи/оглавление) не влияет — контейнер
  просто получается крупнее, чем нужно. Чинить не сейчас; если возьмёмся — скорее всего проще
  нормализовать сам `content` в БД (заменить `"\n \n"` → `"\n\n"` при импорте/миграцией), чем менять
  паттерн split на regex сразу в двух местах на разных платформах.

### Исправлено (после аудита, вне протокола выше)
- Google-логин (`POST /api/login`) не проверял подпись id_token — доверял `user_id`, присланному
  клиентом напрямую. Теперь верифицируется через `google-auth-library` (2026-08-19, для мобильного
  Google Sign-In и заодно для веба).
- `getUserByVKId/GoogleId/TelegramId` — были то же самое, что и SQL-инъекция из аудита выше: точное
  совпадение подобъекта `{"auth.google": {userId}}`. Заменено на `"auth.google.userId": id` (2026-08-19).
- `/api/report` не проверял сессию — `userId` брался из тела запроса как есть. Теперь берётся из
  `getSession()`, без валидной сессии — `401` (2026-08-19).

---

## Аудит от 2026-08-24 (код, не продукт)

Разбор по коду, дополняет продуктовые идеи ниже. Пять блоков по убыванию отношения
пользы к трудозатратам.

### Блок 1. Дешёвые победы

- **`<html lang="en">`** в [src/app/layout.tsx](../../src/app/layout.tsx) на русско-церковнославянском
  сайте — влияет на выдачу, скринридеры и переносы.
- **`public/sitemap.xml` статичен и мёртв** — 2493 URL, сгенерирован внешним сервисом
  mysitemapgenerator, у всех записей `lastmod = 2024-02-20`. Новые чтения и жития в индекс
  не попадают вообще. Для контентного сайта на ~3200 страниц это самая дешёвая органика,
  которую мы не берём. Заменить на `src/app/sitemap.ts`, строящий карту из Mongo по реальным
  `updatedAt`.
- **Поиск отдаёт всё и без ограничений** — [src/app/search/api.ts](../../src/app/search/api.ts)
  делает `$text` без `limit`, без `projection` и без пагинации: на запрос из трёх букв
  возвращает все совпавшие тексты целиком, вместе с полем `content` (тысячи символов на
  документ). UI при этом использует только `id` и `name`. Плюс rate limiting отсутствует —
  вместе это простой способ положить сервер.
- **Утечка sqlite-хендлов** — `init()` из [src/lib/sqlite.ts](../../src/lib/sqlite.ts) создаёт новый
  `Database` при каждом вызове (58 вызовов по коду), `db.close()` встречается один раз. В
  long-running процессе файловые дескрипторы копятся. Нужен синглтон на процесс.
- **`ignoreBuildErrors: true`** в [next.config.js](../../next.config.js) — типы в проде не проверяются
  вообще, при 247 `: any` по коду. Замер на 2026-08-24: `npx tsc --noEmit` даёт **171 ошибку**,
  большая часть — `unknown` из `better-sqlite3.all()` в модуле `nobles`. Снимать флаг можно только
  после разбора этой кучи, но знать её размер полезно уже сейчас.

### Блок 2. Архитектурный рычаг: сайт целиком динамический

Корневой [layout.tsx](../../src/app/layout.tsx) вызывает `verifySession()` и `getItem()` — это
заставляет Next рендерить **каждую** страницу динамически. Ни одной `revalidate` или
`generateStaticParams` в проекте нет (проверено, ноль вхождений): страница жития XII века ходит
в Mongo при каждом заходе анонима.

Что делать: вынести сессию из корневого layout (клиентский компонент или отдельный сегмент),
контентные маршруты — `/reading/[id]`, `/saints/[id]`, `/months/[id]`, `/library/[id]` — перевести
на ISR, инвалидация по хуку из админки при сохранении текста. Это самая крупная разовая выгода по
скорости и нагрузке, и она же делает осмысленным офлайн-кэш в PWA.

Рядом: **шрифты**. `public/fonts` — 1.2 МБ OTF, из них `myFont` тянет ~450 КБ (Regular + Bold)
до первой отрисовки текста на каждой странице чтения. Конвертация в woff2 даёт примерно двукратное
сокращение без потерь; сабсеттинг — больше, но аккуратно, чтобы не потерять церковнославянские
надстрочные знаки.

### Статус блока 2 — сделано 2026-08-24 (шрифты не трогали)

**Сессия убрана из рендера.** Корневой [layout.tsx](../../src/app/layout.tsx) больше не вызывает
`verifySession()`/`getItem()`; сессию забирает [SessionLoader](../../src/app/SessionLoader.tsx) с клиента
через новый `GET /api/session` и кладёт в тот же redux-стор, из которого меню читало её и раньше.
В `auth`-слайс добавлен флаг `isResolved` — без него у залогиненных на мгновение мигала кнопка
«Войти». `SessionChecker`/`SessionDispatcher` удалены как дублирующие. Важно: `SessionLoader` должен
лежать **внутри** `StoreProvider` — иначе react-redux падает уже на этапе пререндера.

**Что теперь кэшируется.** Пререндеренных маршрутов стало 19 против 2 (`/`, `/library`, `/calendar`,
`/triodion`, `/penticostarion`, `/rest-readings`, `/commons`, статические разделы). `/months/[id]` и
`/library/[id]` — ISR по первому запросу (`MISS` → `HIT`, проверено заголовком `x-nextjs-cache`).

Отдельная находка: **`export const revalidate` для динамического сегмента не работает без
`generateStaticParams`** — Next считает такой сегмент полностью динамическим и результат не кэширует
вовсе. Пустой массив (`generateStaticParams = async () => []`) включает генерацию по первому запросу
с последующим кэшированием, при этом сборка не тратит время на 2500 страниц.

**Выборки из базы кэшируются отдельно** ([src/lib/cache.ts](../../src/lib/cache.ts), `unstable_cache` с
тегами). Это то, что спасает страницы, которые остаются динамическими: `/reading/[id]` из-за
`?range`, а `/calendar/[id]`, `/weeks/[id]`, `/triodion/[id]`, `/calculator/[date]` — из-за cookie
языка Библии. Замеры на продовой сборке, первый запрос → последующие:

| маршрут | было | стало |
|---|---|---|
| `/calendar/march-30` | 271 мс | 23 мс |
| `/triodion/post-5-vt` | 435 мс | 33 мс |
| `/reading/dobrotolubie-30` | 78 мс | 18 мс |

Ошибки в кэш не попадают: `cachedTuple` пробрасывает их наружу, иначе сбой базы залипал бы на час.

**Инвалидация.** `POST /api/revalidate` (только админ, в проде без прав — 404) сбрасывает теги;
редакторы текстов и дней в админке дёргают его после сохранения
([src/lib/admin/revalidate.ts](../../src/lib/admin/revalidate.ts)). Отдельная ручка нужна потому, что API
админки живёт в pages-роутере, а `revalidateTag` работает только в app-роутере. Страховка по времени —
час для контента, 5 минут для главной (на ней церковная дата и список последних правок).

**Проверено:** тексты страниц продовой сборки сравнены с версией до изменений — на 14 маршрутах всех
типов единственное расхождение в том, что раньше в HTML попадал `Loading...` из Suspense, а теперь
контент отрендерен сразу. 32 адреса из карты сайта отдают 200. `tsc --noEmit` — 170 ошибок против
171 до начала работы, новых нет.

Что осталось за рамками:
- `/saints/[id]` не трогал — в файлах этого раздела лежит незакоммиченная работа по `nobles`.
- Инвалидация подвешена только на редакторы текстов и дней; книги, месяцы и недели правятся реже —
  их подхватит часовой таймаут.
- `CountMeta` на каждый просмотр страницы пишет в Mongo IP, URL и растущий массив `wasAt`
  ([src/app/api.ts](../../src/app/api.ts), `writeMetaData`). Рендер это не блокирует (запрос уходит с клиента
  после отрисовки), но документ на популярный URL растёт без ограничений, а собирать IP-адреса — это
  ещё и персональные данные. Стоит решить отдельно: ограничить массив или считать агрегатом.

### Блок 3. Продукт: приоритеты внутри фокуса

~~Автопостинг в Telegram написан целиком, но не проверен на реальных данных и крон не включён.~~
Снято 2026-08-24: направление отложено, с сервера `api.telegram.org` недоступен, подробности и
условия возврата — в разделе про автопостинг ниже. Поэтому «доведение до пользователя» двигаем
тем, что не зависит от доступности внешнего API с прод-сервера.

Приоритеты внутри фокуса (уточнение к списку ниже):
1. **iCal-фид** — один route, данные уже есть в `/api/v1/days`, доставка идёт в календарь,
   которым человек уже пользуется. Лучшее отношение пользы к трудозатратам, и с отложенным
   Telegram это теперь первый шаг направления.
2. **PWA** — manifest + service worker; даёт офлайн вебу и частично закрывает iOS-разрыв
   (установка на домашний экран) без затрат на нативное приложение. Осмысленно делать
   **после** блока 2: кэшировать статически отрендеренные страницы намного проще.
3. **TTS** — ~~начинать с русских синаксарей и житий~~ снято 2026-08-24: русских текстов в базе
   нет вообще, посылка была неверной. Разбор и реальный план — в разделе «Озвучка чтений» ниже.

### Блок 4. Что нужно решить, а не откладывать: `nobles` — РЕШЕНО 2026-08-24

129 файлов из ~494 в `src` относятся к родословной. При этом модуль был объявлен экспериментальным
и «не развиваем» — а последний коммит, незакоммиченные изменения в рабочем дереве и четыре новых
скрипта в `package.json` относились именно к нему. Расхождение между заявленным фокусом и тем, куда
уходит время, — самая дорогая позиция в этом списке.

**Решение (2026-08-24):** родословная выведена из-под статуса эксперимента и принята как направление
развития. Расхождение снято в сторону признания второго направления. Вопрос отдельного
репозитория/поддомена остаётся открытым — см. раздел «Родословная (`nobles`)» ниже.

### Блок 5. Гигиена

- **Тесты — СДЕЛАНО 2026-08-24.** 56 тестов на чистой логике: подвижный круг, отступка/преступка,
  диапазоны стихов, церковная дата, нормализация ЦС для поиска, сборка iCal. Запуск — `npm test`
  (`node --test` через `tsx`, без новых зависимостей). CI в
  [.github/workflows/ci.yml](../../.github/workflows/ci.yml) гоняет тесты и линтер;
  `next build` в CI нет намеренно — сборка пререндерит карту сайта и календарную ленту,
  для чего нужна Mongo. `tsc --noEmit` тоже пока не в CI: 169 накопленных ошибок типов.

  Что тесты нашли по дороге:
  - **Сырная седмица не резолвится по дате.** Неделя сыропустная (за 49 дней до Пасхи)
    получает `{week: 0, type: "Fast"}`, а в базе эта седмица лежит как
    `{type: "Triodion", value: 0}` (alias `syrnaja`, семь дней). `getTriodicItem` ничего
    не находит, и на `/calculator` за эти даты Триодь не показывается, хотя тексты есть
    и открываются через `/triodion/{alias}`. Зафиксировано как `todo`-тест в
    [movableCycle.test.ts](../../src/utils/movableCycle.test.ts).
  - **Преступка в 2024 году пропускает четыре седмицы подряд** (позиции 14–17 выпадают,
    18-я начинается с позиции 14). Ветка преступки в коде помечена как непроверенная
    гипотеза — тест фиксирует текущее поведение, но не утверждает, что оно верное.
  - Чистая арифметика подвижного круга вынесена из `lib/calcDay` в
    [utils/movableCycle.ts](../../src/utils/movableCycle.ts): раньше её нельзя было проверить,
    не поднимая подключение к Mongo.
- ~~**Мониторинг**~~ — сведено 2026-09-07: через `src/lib/reportError.ts` теперь идут 162 места
  на сервере и через новый `src/lib/reportClientError.ts` — четыре в браузере. Своим
  `console.error` остались два места, и оба не про исключения. См. «Типы и логи» ниже.
- ~~**Версии**~~ — сверено 2026-09-03: Next 14.2.35 (CVE-2025-29927 закрыт), mongodb 6.21,
  TypeScript 5.9, `next-auth` удалён.
- ~~**Индексы Mongo нигде не заводятся в коде**~~ — заводятся: `src/scripts/ensure-indexes.ts`
  плюс `createIndex` в семи скриптах импорта и сборки. Индексы переживают `mongorestore --drop`
  прогоном скрипта, а не памятью администратора.
- **`NOTES.md`** — в `.gitignore`, в историю не попадал (проверено `git log -- NOTES.md`), но
  root-пароль сервера лежит открытым текстом в рабочем каталоге. Сменить, перейти на ssh-ключи,
  отключить парольный вход для root.

### Статус блока 1 — сделано 2026-08-24

- [x] `lang="ru"` в корневом [layout.tsx](../../src/app/layout.tsx).
- [x] Динамический [src/app/sitemap.ts](../../src/app/sitemap.ts) из Mongo, статический `public/sitemap.xml`
  удалён. 3063 уникальных адреса против 2493 устаревших: тексты (только `ready`/`correcting`/`texted` —
  заготовки `presence` в индекс не идут), дни неподвижного круга, пасхальные дни под своими разделами,
  месяцы, книги, статические разделы. `revalidate = 86400`. Проверено: сборка пререндерит маршрут,
  выборочные адреса всех разделов отдают 200.
- [x] Поиск: сортировка по релевантности, `limit 100`, проекция без `content`
  ([src/app/search/api.ts](../../src/app/search/api.ts)). На запросе «преподобный» ответ упал
  с 294 КБ до 68 КБ (текстовый индекс всё равно построен по названию, тело в выдаче не использовалось).
- [x] Синглтон sqlite-соединения ([src/lib/sqlite.ts](../../src/lib/sqlite.ts)), в dev — через `global`,
  как у MongoClient. Убран единственный `db.close()` в `admin/nobles/sync.ts`, который теперь закрыл бы
  общее соединение.
- [x] `ignoreBuildErrors` — флаг снят 2026-09-07, ошибок типов ноль (было 150). См. раздел
  «Типы и логи» ниже.

Побочная находка: **в базе есть документы с одинаковым `alias`** — 14 дубликатов среди текстов и дней
(`/reading/dobrotolubie-6`, `/calendar/may-05` и другие). Маршрут `/reading/{alias}` отдаёт из них
случайный, второй документ недостижим. В карте сайта дубликаты схлопываются, но саму базу это не чинит —
стоит завести отдельную задачу на проверку уникальности `alias` при сохранении.

---

## Аудит от 2026-09-19

Третий проход, только на чтение; продакшен не затрагивался. Два прежних аудита закрыты почти
полностью, и ниже записано лишь то, чего в них нет или что осталось открытым. Уже лежащее в
`TODO.md` (жадные выражения разметки, сжатие JSON, редирект `/.well-known/`, выгрузка без певческого
корпуса, ключ FCM) не повторяется.

**Общая оценка.** Сильные стороны: глубина корпуса; кэш выборок с тегами; сто файлов тестов на
чистой логике; ноль ошибок типов под CI; параметризованные запросы SQLite и экранированные
`$regex` во всех местах, где строка приходит от пользователя; `react-markdown` без `rehype-raw`;
ручка `/api/revalidate` закрывается при пустом токене и сравнивает за постоянное время; привычка
записывать решения вместе с основанием. Слабые: вход и профиль (блок А), эксплуатация (блок Г),
два роутера с размытыми слоями (блок Д) и отсутствие проверок всего, чему нужна база.

### Статус на 2026-09-19 — исправлено в коде, не выложено

Сделано одним проходом; `npm test` (961 тест, в том числе при `TZ=Europe/Moscow`),
`npm run typecheck` и линтер чисты; ручки проверены запросами к локальной сборке.

- **Блок А.** Идентификатор пользователя во всех трёх ветках входа сервер получает от
  провайдера и никогда из тела запроса: у VK — предъявлением токена самому VK
  ([verifyVkToken.ts](../../src/lib/authorize/verifyVkToken.ts)), у Telegram — из полей, по которым он же
  собирает строку подписи ([telegram.ts](../../src/lib/authorize/telegram.ts): срок `auth_date`, сравнение за
  постоянное время, 503 без токена бота). `POST /api/profile` правит запись из сессии и только по
  белому списку ([profileFields.ts](../../src/lib/authorize/profileFields.ts)); почта, занятая другой
  записью, отвергается. Привязки входа в форме профиля показываются, но не набираются.
  `CODE_VERIFIER` из окружения больше никуда не передаётся — VK ID SDK порождает значение сам.
- **Блок Б.** Адрес клиента — `x-real-ip` либо последний элемент `x-forwarded-for`
  ([rateLimit.ts](../../src/lib/rateLimit.ts)); капча со сроком, пределом и одной попыткой; лимиты на
  `/api/captcha`, `/api/contact`, `/api/v1/pdf`; заголовки безопасности в `next.config.js`
  (`frame-ancestors 'self'` всюду, кроме `/embed`); `verifySession()` и `viewer()` сверяют строку
  `sessions`; срок сессии считается от времени сервера, в базу кладётся только нужное;
  `nobles/sync.ts` под `checkRightsBack`, `test.ts` и `value.json` удалены; проверка `Origin` у
  изменяющих запросов с сессией ([originCheck.ts](../../src/lib/originCheck.ts)); пределы полей в
  `/api/report`, `/api/texting`, `/api/user-notes`.
- **Блок В.** Дата Пасхи строится из чисел полночью UTC (`easterDateUtc` в
  [movableCycle.ts](../../src/utils/movableCycle.ts)). Сдвиг подтвердился как настоящая ошибка:
  `getWeekAndDay` округляет через `Math.ceil`, и три часа разницы давали лишний день на весь год.
- **Блок Г (то, что в репозитории).** `package-lock.json` убран из `.gitignore`, CI и
  `rebuild-remote.sh` ставят через `npm ci`; из `typikon-web.service` убраны строки next-auth.
- **Блок Д.** Выражения разметки сведены в [lib/markup/patterns.ts](../../src/lib/markup/patterns.ts) с
  тестами; жадные заменены, подпись у метки необязательна (закрывает запись из `TODO.md`).

**Не проверено вживую:** вход через VK и Telegram — локально провайдеры недоступны (у VK источник
`localhost` не зарегистрирован, токена бота в окружении разработки нет). Проверить на сервере
сразу после выкладки; запрос к `id.vk.com/oauth2/user_info` с сервера ранее не делался.

**После выкладки:** погасить все сессии (коллекция `sessions`), проверить `typikon-users.users` на
посторонние `roles`/`isAdmin` и на чужие `auth.*`, привязанные через прежнюю форму профиля.

**Осталось из раздела:** полная CSP (требует проверки входа в браузере); подтверждение почты
письмом; обёртка `withAdmin` вместо 60 копий `SHOW_ADMIN`; схема для правил прихода; всё, что на
сервере (ключи ssh, сборка вне рабочего каталога, резервные копии, версия Node, `X-Real-IP` в nginx
и закрытый порт Node); блоки Д и Е, кроме разметки.

### А. Вход и профиль — критическое

Три находки, каждая из которых даёт захват чужой учётной записи либо прав администратора; все
три перепроверены чтением кода. Затронуты ветки VK и Telegram в
[src/app/api/login/route.ts](../../src/app/api/login/route.ts) и ручка
[src/app/api/profile/route.ts](../../src/app/api/profile/route.ts) с `setItem` из
[src/app/profile/api.ts](../../src/app/profile/api.ts). По существу это та же ошибка, что была исправлена
для Google 2026-08-19: серверу присылают то, что он обязан установить сам.

Механика и порядок исправления записаны в `scratch/AUDIT-2026-09-19-security.md` — каталог
`scratch/` стоит в `.gitignore`, и описание не попадёт в репозиторий прежде исправления. После
исправления перенести сюда по образцу «Исправлено» из аудита 2026-08-13.

Четвёртое, того же рода, но слабее: `CODE_VERIFIER` из окружения передаётся пропсом в клиентский
`AuthorizeChecker.tsx` ([layout.tsx](../../src/app/layout.tsx), [login/page.tsx](../../src/app/login/page.tsx)).
Постоянное и общее для всех проверочное значение PKCE ничего не защищает; снимается вместе с
переносом обмена кода VK на сервер.

**Это первая очередь, прежде всего остального в разделе.**

### Б. Безопасность — высокое и среднее

- **Ограничение частоты и капча обходятся подменой `x-forwarded-for`.**
  [rateLimit.ts](../../src/lib/rateLimit.ts) доверяет первому элементу заголовка, а за nginx с
  `$proxy_add_x_forwarded_for` первый элемент — присланный клиентом. Ручки капчи и обратной связи
  берут ключом заголовок целиком. Брать следует `X-Real-IP`, выставленный nginx. Заодно проверить,
  что порт Node недоступен снаружи: `typikon-web.service` запускает его на 80.
- **Хранилище капчи** ([captcha.ts](../../src/lib/captcha.ts)) живёт в памяти без срока и предела и держит
  буфер картинки на запись; неверный ответ запись не гасит, так что одну капчу можно подбирать
  сколько угодно. `/api/captcha`, `/api/contact`, `/api/v1/pdf` и `/api/client-errors` без
  ограничения частоты — форма обратной связи годится для рассылки через наш SMTP, а сборка PDF
  из произвольного JSON — для загрузки процессора.
- **Заголовков безопасности нет** ([next.config.js](../../next.config.js)): ни CSP, ни
  `frame-ancestors 'self'` вне `/embed`, ни `nosniff`, HSTS, `Referrer-Policy`. `/admin` и
  `/profile` можно поместить в чужую рамку. Главный конфиг nginx в репозитории не лежит — сверить,
  не выставлено ли это там.
- **Выход не гасит сессию на маршрутах app-роутера.** `verifySession()` и `viewer()`
  ([authorization.ts](../../src/lib/authorize/authorization.ts), [rights-server.ts](../../src/lib/rights-server.ts))
  сверяют только JWT, строку в `sessions` не ищут; `getSession()` и `verifySessionBack()` — ищут.
  Уведённый токен живёт до часа на `/api/admin/roles`, приходских ручках и `/api/revalidate`.
- **Срок сессии считается из клиентских `timestamp` и `expires_in`**
  ([sessions.ts](../../src/lib/authorize/sessions.ts)); в базу кладётся весь `body.data` вместе с токенами
  провайдера, и `/api/prolong` отдаёт их странице. Нужны серверное время, предел срока и отбор полей.
- **`src/pages/api/admin/nobles/sync.ts` — единственная живая админ-ручка без `checkRightsBack`**;
  сейчас она лишь повторяет `CREATE … IF NOT EXISTS`, но стоит открытой. Рядом `test.ts` с голым
  `return` и `value.json` — мёртвый код в каталоге маршрутов.
- **Проверка `SHOW_ADMIN` скопирована в 60 файлов**, а `src/middleware.ts` для `/api/admin` не
  проверяет ничего. Ошибка аудита 2026-08-13 (28 ручек с непрерванным обработчиком) — следствие
  того же устройства; одна обёртка `withAdmin(handler)` снимает сам класс ошибок.
- Нет проверки `Origin` на изменяющих запросах (держится на одном `SameSite=Lax`); нет пределов
  длины и проверки типов полей в `/api/report`, `/api/texting`, `/api/user-notes`; правила прихода
  (`/api/parish/[slug]/settings`) сохраняются без проверки схемы — право проверено верно, страдает
  только целостность данных.

### В. Вероятная ошибка расчёта дня

[calcDay.ts](../../src/lib/calcDay.ts) строит дату Пасхи строкой, в которой месяц дополнен нулём, а день —
нет: `"2027-05-2"`. Такая строка не ISO, и V8 разбирает её по местному времени, тогда как входная
дата вида `"2027-05-02"` разбирается по UTC. Проверено: при `TZ=Europe/Moscow` первая даёт
`2027-05-01T21:00Z`, вторая — `2027-05-02T00:00Z`. Затронуты годы, где Пасха приходится на 1–9
число: 2024, 2027, 2029, 2032, 2034, 2037, 2040. Приводит ли трёхчасовой сдвиг к ошибке на день,
зависит от округления в `getWeekAndDay` — это не проверено. Записанная в аудите 2026-08-24
аномалия преступки как раз 2024 года может быть отсюда же; проверяется одним тестом.

Того же рода: пасхалия считается тремя способами (`date-easter` в `calcDay.ts`, `temples.ts` и
`calculator/Editor.tsx`; обёртка `getPaschaDate` в `utils/lectionaryCycle.ts`; независимый
`paschaJdn` в `utils/chronology.ts`), а сдвиг «13 дней», верный лишь для 1900–2099 годов, зашит в
пяти местах. Предложение — один модуль церковного календаря: Пасха, сдвиг стиля по веку, сборка
даты из чисел, а не из строки; и тесты `calcDay`, которых сейчас нет вовсе.

### Г. Эксплуатация

- **Выкладка.** Одиннадцать скриптов ходят на сервер через `sshpass` с паролем из `.env.release`,
  ключи узла не закреплены. `rebuild-remote.sh` делает `git pull && npm i -f && next build` в
  каталоге работающего процесса: на время сборки `.next` переписывается под живым сервером, отката
  нет.
- **`package-lock.json` одновременно отслеживается git и стоит в `.gitignore`.** Последняя его
  правка — 7 сентября, `package.json` менялся 17-го; CI ставит зависимости через `npm install`, и
  в его комментарии сказано, что файла в репозитории нет. При диапазонах `^` сборка
  невоспроизводима: CI и прод могут стоять на разных версиях.
- **Node.** В `typikon-web.service` — v20.20.2, в `ci.yml` — 22 с пометкой «та же версия, что на
  сервере»; ни `engines`, ни `.nvmrc` нет. В том же service-файле остались `NEXTAUTH_URL` с чужим
  доменом (и кириллической «о» на конце) и `AUTH_TRUST_HOST` — next-auth удалён 2026-08-24.
- **Резервные копии.** `mongod_dump.sh` читает окружение из `/home/admin/typikon-web/`, а проект
  лежит в `/var/www/typikon.su/typikon-web/` — следует убедиться, что крон действительно что-то
  снимает. Копии хранятся семь дней и на том же сервере; внешней копии нет. Между тем
  `typikon-users` (помянник, приходы, заявки) существует ТОЛЬКО на проде. В роадмапе до этого
  раздела резервные копии не упоминались ни разу.
- **Наблюдение.** Ручки проверки живости нет; `reportError` пишет в journald, оповещений нет.
- **CI не собирает проект** и не проверяет ничего, чему нужна база: авторизацию, поиск, расчёт
  дня. Дымового прохода после выкладки нет, хотя руками он делался (32 адреса, 2026-08-24).

### Д. Архитектура и качество кода

- **Два роутера.** В `src/pages/api` остаётся 100 файлов (55 — админка, около 39 — v1), в
  `src/app/api` — 105 `route.ts`; v1 и v2 отдают одни и те же ресурсы. Зависимости идут и в
  обратную сторону: `src/pages` берёт `@/app/**/api` 18 раз, `src/lib` берёт `@/app` трижды
  (`rights-server.ts`, `parish/access.ts`, `saintFacts.ts`). `client.db("typikon")` повторён 146 раз.
- **Разбор разметки повторён в четырёх компонентах** — `ReadingContent.tsx`, `DayPartReading.tsx`
  (дважды, второй раз на четвёртом уровне вложенности), `TextPart.tsx`, `ContentRandom.tsx` — и
  нигде не покрыт тестом. Ошибка жадных выражений из `TODO.md` — прямое следствие: её чинить в
  каждом месте отдельно.
- **`any`.** 537 `: any` и 178 `as any` (было 247 на 2026-08-24 — код вырос быстрее, чем
  типизировался). `cachedTuple` возвращает `[any, any]`, и на этой границе теряют тип все 72
  кэшированные выборки. Очаг — `src/lib/api/v2/serialize.ts`: 1175 строк и 76 `any`.
- **Линтер.** ESLint 8.32 и `eslint-config-next` 13.1.2 при Next 14.2; typescript-eslint нет.
- **Данные.** N+1 в `src/lib/pericopes.ts` (по `findOne` на зачало, затем ещё раз на поле дня);
  индексы `temples`, `memories`, `memory_saint_links`, `lexems`, `verses`, `name_index`,
  `dedications` заводятся в скриптах импорта, а не в `ensure-indexes.ts`, — свежая база зависит от
  того, какие скрипты прогнаны.
- **Сборка для браузера.** `react-modal` статически входит в `ReadingContent.tsx` — самую частую
  страницу; `family-chart` и шесть карт на `ol` — без `dynamic()`. `abcjs` и `html2pdf.js`
  подгружаются лениво, как и следует. `nodemailer-smtp-transport` не импортируется нигде.
- **Redux** держит один объект сессии на восемь потребителей; довольно контекста.
- **Крупные компоненты:** редактор текста в админке (770 строк, 47 хуков), `ReadingContent.tsx`
  (723 и 34).
- Нет `not-found.tsx` и `global-error.tsx`; метаданные заданы у 96 страниц из 150.
- **Из прежних аудитов открыто:** `writeMetaData` с растущим `wasAt` и IP-адресами;
  `.git` 263 МБ; пароль root в `NOTES.md`.

### Е. Документация

Этот файл — 630 КБ и шесть с лишним тысяч строк — стал журналом сделанного: открытые пункты
рассеяны по сорока разделам, и раздел «Что осталось после прохода 2026-09-05» появился именно
поэтому. `README.md` описывает состояние 2023 года. Архитектурной записки нет, и порядок без малого
ста npm-скриптов вне раздела «Обслуживание базы» нигде не изложен.

### Что делать: инженерная часть

Порядок: блок А; блок Б; проверка резервных копий; блок В; прочее по списку.

1. **Выкладка:** ssh-ключи вместо `sshpass`; `package-lock.json` из `.gitignore` убрать и ставить
   через `npm ci` и в CI, и на сервере; сборка в отдельный каталог с переключением ссылкой (либо
   `output: 'standalone'`), откат одной командой; `engines` и `.nvmrc`.
2. **Резервные копии:** внешнее хранилище, отдельный дамп `typikon-users`, проверка
   восстановления; возраст последней копии — строкой в панели здоровья, раз уж она есть.
3. **Mongo-служба в CI** с малым фикстурным дампом: тогда в CI входят `next build`, тесты
   авторизации, поиска и `calcDay` и дымовой проход по 30–40 адресам (`npm run smoke`), годный и
   после выкладки.
4. **Общие модули:** `src/lib/markup` с тестами на все четыре места разбора; модуль церковного
   календаря (блок В); `getDb()`/`getUsersDb()` и типизированный `cached<T>`.
5. **Один роутер:** админ-ручки перенести в app-роутер под `withAdmin`; для v1 назначить срок
   снятия по счётчикам, которые middleware уже ведёт.
6. **Разделить этот файл:** открытое (в пределах трёхсот строк) и архив сделанного по разделам;
   обновить `README.md`, завести `ARCHITECTURE.md` со слоями и порядком выкладки.

Продуктовая часть — в разделе «Идеи развития продукта», подраздел «Предложения аудита 2026-09-19».

---

## Типы и логи: снят ignoreBuildErrors, сведён сбор ошибок (2026-09-07)

Первая работа выпуска 6.2 — то, что стояло обещанием на `/about` с шестого выпуска
(«Типизация») и открытым пунктом в блоке 1 аудита.

### Проверка типов включена

Было 150 ошибок `tsc --noEmit` и `ignoreBuildErrors: true` в [next.config.js](../../next.config.js) —
то есть типы в проде не проверялись вовсе. Стало ноль, флаг снят, `next build` проходит проверку
и собирает 133 статические страницы. Чтобы долг не набрался снова, `npm run typecheck` добавлен
в [CI](../../.github/workflows/ci.yml): базы ему не нужно, и он ловит ошибку раньше сборки.

Разбор по родам, а не по файлам:

- **49 ошибок — мёртвые `@ts-expect-error`.** `{/* @ts-expect-error Async Server Component */}`
  над каждым асинхронным компонентом в `<Suspense>` — приём Next 13, в Next 14 не нужный.
  Сам TypeScript и объявлял их лишними; сняты в 48 файлах.
- **37 ошибок — `unknown` из better-sqlite3.** `get()` и `all()` объявлены отдающими `unknown`,
  и всякое обращение к полю строки было ошибкой. Заведены строчные типы
  [src/lib/nobles/types.ts](../../src/lib/nobles/types.ts) — сняты не с употребления, а с самой базы
  (`PRAGMA table_info`), семь таблиц. Отсюда же, попутно, разобраны два места, где в массив
  клался `undefined` от ненайденной строки, а следующий `find()` по нему падал.
- **40 ошибок — неявный `any` в пропсах и обработчиках.** Пять полей-подборщиков редактора
  персоны получили общий договор [PickerProps](../../src/app/admin/nobles/[id]/types.ts): устроены они
  одинаково, и описан он теперь один раз.
- **Остальное — россыпь.** Объявления для `trek-captcha` и `pagebreak` в html2pdf (обоих не было),
  расширение `Window` (стояло в `src/types/extension.d.ts`, но файл не был модулем — и объявление
  не работало вовсе), типы `pdf-lib`, узкие места с `null`.

### Что проверка нашла, кроме типов

Четыре ошибки, из-за которых она и стоит того:

- **Цветная Триодь не передавала язык Библии.** `/penticostarion/[id]` звал `getItem(id)` без
  второго довода, тогда как `/triodion/[id]` и `/weeks/[id]` передают язык из cookie. Зачала
  резолвились с `lang === undefined`, всегда падали в церковнославянский фолбек — и страница
  считала это подменой издания. Появилось при работе над Библией в 6.0; типом поймано теперь.
- **`console("load failed")`** в обработчике неудачной загрузки html2pdf: `console` не функция,
  и вместо сообщения вылетал бы TypeError.
- **Долгота под ключом `code`.** В `src/app/places/common/data2.json` у точки «Ново Место»
  вместо `lon` стоит `code: 16.2325`, и точка уезжала в NaN. Значение не тронуто, исправлено
  имя ключа: 47.8089/16.2325 — Винер-Нойштадт, он же «новый город».
- **Пропс `id` у редактора дня** объявлен обязательным, не используется в теле и не передаётся
  единственным вызывающим. Снят.

Ещё два места помечены прямо в коде и оставлены как есть: `fNotes.includes(undefined)` в
редакторе текста — проверка, сработать не могущая (`footNotesToArray` пустых значений не отдаёт),
и крестик в `FamilyItem`, который снимает показ рода, но не сам `familyId`, — в четырёх соседних
подборщиках он зовёт `setValue(null)`.

### Сбор ошибок сведён в одну точку

К `src/lib/reportError.ts` было подключено два места, а по коду вне `src/scripts` стояло
168 `console.error`, из них 142 — голое `console.error(e)` без всякого указания, где это
случилось. Теперь через `reportError` идут 163 места, и в журнал попадает разбираемая строка
с полем `where` (модуль и функция) и, где есть что положить, `extra`.

Браузерная сторона закрыта тем же приёмом: [reportClientError](../../src/lib/reportClientError.ts)
шлёт ошибку ручке `/api/client-errors`, которая и зовёт `reportError`. Прежде этим путём
пользовалась одна граница ошибок страницы, а всё, что перехвачено раньше неё — не загрузился
модуль, не пришли точки карты, — оставалось в консоли посетителя.

Своим `console.error` остались два места, и оба не про исключения: строка состояния от службы
устава и сводка расхождений в данных напевов.

**Маркер `server-only` из `reportError` пришлось снять**, и это стоит знать. Пакета такого в
проекте нет — его подменяет сборщик Next, — поэтому вне сборки, под `tsx` в тестах, импорт не
резолвится: с маркером падали четыре набора тестов, чьи модули дотягиваются до `rulesDb`. Взамен
уговор, записанный в самом файле: секретов там не держим, и DSN Sentry, когда дойдёт до него,
читается только на сервере.

### Следы отладки сняты, и два из них писали в журнал лишнее

Вне `src/scripts` стояло 76 `console.log`. Разобраны по роду:

- **51 «mongodb error»** в блоках `catch` — это ошибки, а не следы: переведены в `reportError`
  вместе с остальными.
- **Тело запроса на `POST /api/login`** писалось в журнал целиком, а в нём приходит `id_token`
  Google. Снято; в коде на этом месте стоит объяснение, чтобы не вернулось. Аудит 2026-08-13
  считал утечку `id_token` закрытой вместе с next-auth — она пережила переезд на свой логин.
- **`console.log(err, process.env.EMAIL, process.env.EMAIL_PASSWORD)`** в ручке обратной связи:
  при неудачной отправке письма пароль почтового ящика уходил в journald. Заменено на `reportError`
  без учётных данных.
- Остальное — дампы значений (`console.log(d)`, `console.log(value)`, `console.log(alias)`,
  тело DELETE `/api/report`, перечень супружеств при вставке) и два уведомления
  «CSS highlight is not supported». Сняты.
- Попутно снят обработчик клика по карте в `/places/common`: он не делал ничего, кроме записи
  в консоль. Кнопка удаления правки в админке следа лишилась, но осталась заглушкой — ручки
  удаления правок нет, и это записано на месте.

Осталась одна запись через `console.log`, и она намеренная: строка об устаревшем API v1
(`src/lib/api/v1Deprecation.ts`), на неё есть тест.

### Ключи списков и подборщик рода

- **React ругался на отсутствующий `key`** на всякой странице дня и чтения: замены
  `react-string-replace` (святой, место, сноска, киноварь) возвращали элементы без ключей.
  Расставлены по образцу, уже принятому в `ReadingContent`: `` key={`kind-${i}-${offset}`} ``.
  Заодно снят `eslint-disable react/jsx-key` над деревьями правлений в родословной.
- **Крестик у поля «Родовое имя»** в редакторе персоны снимал показ рода, но не сам `familyId`:
  в четырёх соседних подборщиках он зовёт `setValue(null)`, а здесь — `setFather(null)`.
  Поправлено, и добавлена ветка `else`, возвращающая поле к поиску, — тоже как у соседей.

### Node поднят до 22

На сервере уже 22, а в проекте стояли `@types/node` 18.11 и Node 20 в CI. Типы подняты до
22.20, версия в [CI](../../.github/workflows/ci.yml) — до 22. Заодно ушли два обхода, стоявшие
из-за расхождения старого `Buffer` с параметризованным `Uint8Array` из TypeScript 5.9.

**`typikon-web.service` не тронут**: в нём прописан путь `/home/admin/.nvm/versions/node/v20.20.2/bin`,
а точная версия 22.x на сервере неизвестна — гадать про путь к запуску службы нельзя.

### Проверено

`tsc --noEmit` — ноль ошибок; `npm test` — 746 тестов, все проходят; `npx eslint src` — ошибок
нет, 38 прежних предупреждений; `next build` — проходит с включённой проверкой типов, 133
статические страницы. В браузере: `/penticostarion/pascha` отдаёт румынское издание при
`bibleLang=ro` (прежде всегда славянское), предупреждения про `key` в консоли больше нет.
Ничего не выкладывалось.

### Попутно: этот файл

В ROADMAP.md дословно повторялся кусок в 1 271 строку — разделы от «Румынской синодальной 1914»
до «Церковнославянского набора» стояли дважды. Копии были не одинаковы: младшая держала разбор
частных кодов и сверку начертаний с Мономахом (таблица долей общих пикселей), которых в старшей
нет. Уникальное перенесено в живой раздел, дубль снят: 6 165 строк стало 4 928.

---
